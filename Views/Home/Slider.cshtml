@model IEnumerable<DataLayer.Page>

<!-- slider -->
<div class="news-slider border-radius">
    <div id="slider-one" class="owl-carousel">
        @foreach (var item in Model)
        {
            <!-- each item -->
            <div class="item">
                <img src="~/PageImages/@item.PageImageName">
                <div class="left-side">
                    <h2> <a href="/News/@item.PageId">@item.PageTitle</a> </h2>
                    <p>
                        @Html.Raw(item.PageText)
                    </p>
                    <span> <i class="fa fa-calendar"></i> انتشار : @item.PageCreateDate.ToShamsi()</span>
                </div>
                <div class="clearfix"></div>
            </div>
            <!-- end each item -->
        }

    </div>
    <a href="/Archive" class="news-archive"> <i class="icons icon-doc-1"></i> آرشیو کامل خبرها </a>
</div>
<!-- End slider -->
